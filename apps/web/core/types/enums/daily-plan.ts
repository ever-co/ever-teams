export enum IDailyPlanModeEnum {
	TODAY = 'today',
	TOMORROW = 'tomorrow',
	CUSTOM = 'custom'
}
export enum DailyPlanStatusEnum {
	OPEN = 'open',
	IN_PROGRESS = 'in-progress',
	COMPLETED = 'completed'
}
