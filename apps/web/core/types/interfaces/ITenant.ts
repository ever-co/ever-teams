export interface ITenant {
	name: string;
	logo: string;
	id: string;
	createdAt: string;
	updatedAt: string;
}

interface ITenant {
	id: string;
	createdAt: string;
	updatedAt: string;
	name: string;
	logo: string;
}
