import type { NextPage } from "next";

const Home: NextPage = () => {
  return <section>
    <h1 className="text-3xl font-bold flex items-center justify-center h-screen">Gauzy Teams</h1>    
  </section>
};

export default Home;
