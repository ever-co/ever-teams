name: Build and Publish ChatGPT App Docker Images Stage

on:
  push:
    branches: [stage]
    paths:
      - 'apps/chatgpt/**'
      - '.github/workflows/chatgpt.docker-build-publish-stage.yml'

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  ever-teams-chatgpt:
    permissions:
      contents: read
      packages: write
    runs-on: ${{ vars.RUNNER_LINUX_X64_4 || 'ubuntu-latest' }}

    environment: stage

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v7

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN || github.token }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/chatgpt/Dockerfile
          load: true
          platforms: linux/amd64
          tags: |
            ghcr.io/ever-co/ever-teams-chatgpt-stage:latest
            registry.digitalocean.com/ever/ever-teams-chatgpt-stage:latest
          cache-from: type=registry,ref=ghcr.io/ever-co/ever-teams-chatgpt-stage:latest
          cache-to: type=inline
          build-args: |
            NODE_ENV=staging

      - name: Install doctl
        if: ${{ vars.DO_ENABLED == 'true' }}
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry with short-lived credentials
        if: ${{ vars.DO_ENABLED == 'true' }}
        run: doctl registry login --expiry-seconds 3600

      - name: Push to DigitalOcean Registry
        if: ${{ vars.DO_ENABLED == 'true' }}
        run: |
          docker push registry.digitalocean.com/ever/ever-teams-chatgpt-stage:latest

      - name: Push to Github Registry
        run: |
          docker push ghcr.io/ever-co/ever-teams-chatgpt-stage:latest
